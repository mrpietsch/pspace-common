language: java

jdk:
  - oraclejdk8

build:
  pre_ci:
    - mkdir -p shippable/testresults
  ci:
    - ./gradlew clean check 
    - ./gradlew-ssl uploadArchives
  post_ci:
    - cp -r build/test-results/* shippable/testresults/
  
